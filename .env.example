# Float runs entirely against providers injected by Nimiq Pay.
# No API keys are required to build or run the app.

# Optional: pin a default validator address shown to first-time stakers.
NEXT_PUBLIC_DEFAULT_VALIDATOR=
